# TODO(P1 — TBD): Implement auth service
# Functions to implement:
#   hash_password(plain: str) -> str
#   verify_password(plain: str, hashed: str) -> bool
#   issue_tokens(user_id: str) -> tuple[str, str]
#   rotate_refresh(old_token: str) -> tuple[str, str]
