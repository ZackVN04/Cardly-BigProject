# TODO(P7 — Khanh): Implement review service
# Functions to implement:
#   start_review_session(processing_id: str, user_id: str) -> JsonReviewSession
#   apply_edits(session_id: str, edits: list[EditOperation]) -> JsonReviewSession
#   validate_pre_confirm(session: JsonReviewSession) -> tuple[bool, list, list]
#   finalize_document(session: JsonReviewSession) -> FinalizedDocument
