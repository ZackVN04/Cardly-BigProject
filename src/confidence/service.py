# TODO(P6 — Nhân Tài): Implement confidence scoring service
# Functions to implement:
#   score_document(processing_id: str) -> ConfidenceReport
#   classify_field(score: float) -> ConfidenceClass
#   log_stage_history(processing_id, stage, status, details) -> ProcessingHistory
#   get_full_document_state(processing_id: str) -> DocumentFullStateResponse
