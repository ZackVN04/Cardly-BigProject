# TODO(P3 — Phú Phàm): Implement preprocessing service
# Functions to implement:
#   normalize_dpi(image_path: str, processing_id: str) -> PreprocessedImage
#   fix_orientation(img) -> tuple[img, int]
#   adjust_brightness_contrast(img) -> tuple[img, float, float]
#   convert_format(img, output_format: str) -> img
